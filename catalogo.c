#include <stdio.h>
#include "catalogolib.h"


int main(void) {

    char query[MAX_CHAR] = "call of duty,,,, #       fps         t ,        # shooter, $2019";
    analisiQuery(query);

    return 0;
}
